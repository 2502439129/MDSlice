# MD 结构说明（渲染机制）

---
title: MD 结构说明
version: V1.0.0
updated: 2026-09-16
scope: 章节切分 / 锚点 / 表格 / 容器 / 缩进的实现规则
---

> [!INFO] **这份文档讲「机制」**
> 使用方式见 [`README.md`](README.md)；这里说明渲染器本身如何处理 Markdown 结构、边界在哪里、什么写法会被怎么渲染。
> 本文件同样用应用自身的语法写成，可直接拖进应用查看渲染效果。

## 一、页面模型

### 1.1 切页规则

| 层级 | 是否切页 | 出现在 | 页面内容 |
|---|---|---|---|
| `#` H1 | 是 | 左栏目录 | **整篇**（含所有子章节） |
| `##` H2 | 是 | 左栏目录 | 本章及其全部子章节 |
| `###` H3 | 是 | 左栏目录 | 仅本章节 |
| `####`～`######` | 否 | 右栏页内目录 | 留在所属页面内 |

1. **代码围栏内的 `#` 不参与切页**——bash 注释、示例里演示的 Markdown 都安全
2. 首个标题之前的内容并入第一章；全篇无标题时作为单章「全文」
3. 每个章节是一个独立的 DOM 容器（`<div class="section" id="sec-…">`），切换即显隐 + 淡入；多标签页时每个标签页各有一套容器（`.pane` / `.nav-tree` / `.toc-list`），未激活的那套 `display:none`，所以各自的滚动与折叠状态互不影响
4. H1 / H2 页会**同时激活其所有后代章节块**，所以「进一章看到全部内容」是自然结果

### 1.2 目录树

1. 由章节层级构建：用栈归并，遇到同级或更高级就弹栈，因此**支持跳级**（如 H1 直接接 H3、文档以 H2 开头）
2. 只有含子章节的行才渲染折叠箭头；叶子行保留等宽占位，保证同层文字对齐
3. **点行文字** = 切页；**点箭头** = 折叠（箭头用 `stopPropagation` 隔离，两者互不干扰）
4. 切到子章节时，其所有祖先会自动展开
5. 「全部折叠 / 全部展开」一次性切换整棵树（折叠状态由 `.toc-sub.collapsed` 与 `.caret.collapsed` 共同表达）

### 1.3 右栏页内目录

1. **仅 `###` 页生成**；取自该页渲染结果中的 `h4[id] / h5[id] / h6[id]`
2. 层级 = `h4` 一级、`h5` 二级、`h6` 三级
3. 页面没有小节时整栏隐藏，并回收 grid 列让正文变宽
4. 用 `IntersectionObserver` 做滚动高亮

## 二、标题级元数据

### 2.1 识别条件（四条同时满足）

1. 标题之后的第一处**非空行**是 `---`（≥3 个短横线）
2. 直到下一个 `---` 之间的**每一行**都形如 `key: value`（key 以字母 / 中文 / 下划线开头，半角冒号）
3. 存在闭合的 `---`
4. 至少有一个键值对

任一条不满足 → 整段**按普通内容渲染**（`---` 变成分隔线）。

### 2.2 渲染结果

| 元数据 | 效果 |
|---|---|
| 任意键值 | 标题下生成元数据卡；**首个章节的 `title` 跳过**（已用作页面标题与顶栏品牌名） |
| `operationType` | 内置配色的徽章键，可选 `[add]` / `[update]` / `[get]` / `[delete]` / `[auth]` / `[other]`，依次为绿 / 黄 / 蓝 / 红 / 灰 / 灰 |
| 任意 `[文本]` | 默认蓝色徽章 |
| `deprecated: true` | 标题与左栏出现「弃用」徽章，并在页顶**自动插入** DANGER 提示；`replacement` 的值会写进提示正文 |

> [!WARNING] **不要手写弃用提示**
> `deprecated: true` 已经自动生成一条，手写会得到两条。

## 三、章节 id 与锚点

### 3.1 id 生成（slug）

处理顺序：转小写 → 删除 Markdown 强调类字符（反引号、`*`、`_`、`~`、`[`、`]`、`(`、`)`、`#`）→ 其余非「字母 / 数字 / 下划线 / 中文」字符换成 `-` → 掐掉首尾 `-`；结果为空则用 `sec`。同名标题自动追加 `-2`、`-3`…

| 标题 | id |
|---|---|
| `### Installation` | `installation` |
| `#### 使用步骤` | `使用步骤`（第二次出现为 `使用步骤-2`） |
| `## 三、data_loaders 模块` | `三-dataloaders-模块` |
| `### A.1 校验规则` | `a-1-校验规则` |

> [!INFO] **下划线会消失**
> slug 把 `_` 当作 Markdown 强调符号一并删除，所以 `data_loaders` → `dataloaders`。写跨章节链接时，请以渲染后的 id 为准（点标题右侧 `#` 可看到实际 id）。

### 3.2 文档内链接的解析顺序

点击 `[文字](#片段)` 时按四步降级，命中即跳转：

1. 原始片段（先做 `decodeURIComponent`）与某个 id **完全相同**
2. 片段 slug 化后相同
3. 双方去掉全部 `-` 后相同
4. **唯一前缀命中**：片段的紧凑形式是某个 id 的前缀，且只命中一个

全部失败则不拦截，交回浏览器默认行为。由于先查 `document.getElementById`，指向 `H4 / H5` 的**页内锚点**也能正确滚动；锚点在别的章节时会**先切章再定位**。

## 四、表格

### 4.1 列语义自动识别

仅当表头命中已知列名（`字段` / `类型` / `必填` / `示例值` / `默认值` / `说明` / `参数` / `名称`）时才启用，而且**只作用于「整格恰好是一个行内代码」的单元格**：

| 列名 | 条件 | 渲染 |
|---|---|---|
| 必填 | 整格是行内代码 | 一律渲染「必填」徽章（**实际取值请写 `[[必填]]` / `[[选填]]`**） |
| 类型 | 整格是行内代码 | 灰色等宽 |
| 示例值 / 默认值 | 整格是行内代码，且形如 `"str"` `[arr]` `true` `123` | 橙色等宽；其他情况为加粗等宽 |
| 其他列 | 整格是行内代码 | 加粗等宽 |

表头不在名单里的普通表格不受影响。

### 4.2 `└` 折叠层级

1. 首列行首的树形字符 `└ ├ │ ┌ ┬ ─` **每个计一层**（`└└` 为两层）
2. 父行 = 上方第一个层级更小的行
3. 有子行的行渲染折叠按钮并显示子项数量，表格上方出现「共 N 个嵌套字段」与「全部折叠 / 全部展开」
4. 折叠实现为给后代行加 `.is-hidden`；`@media print` 时强制展开，保证打印完整

### 4.3 溢出与窄屏

1. 表格外层可横向滚动，避免撑破正文列宽
2. ≤820px 时表格卡片化：每格用 `data-label` 前缀显示列名，首列树形单元格单独处理缩进

## 五、容器与提示框

### 5.1 引用与 Callout

| 写法 | 渲染 |
|---|---|
| `> 普通引用` | 无图标的中性浅底卡片 |
| `> [!INFO]` | 蓝色信息卡片 |
| `> [!WARNING]` | 黄色警告卡片 |
| `> [!DANGER]` | 红色危险卡片 |
| `> [!TIP]` | 绿色建议卡片 |

渲染器还认识 `NOTE` / `IMPORTANT` / `SUCCESS` / `CAUTION` / `ATTENTION` / `ERROR` / `FAILURE` / `BUG` 等别名（自动映射到上面五种）；**建议只用上面四种**，主题与图标最完整。Callout 内出现空行会中断该块，正文每段需继续以 `>` 开头。

### 5.2 `:::` 容器

| 标记 | 作用 | 子标记 | 备注 |
|---|---|---|---|
| `:::tabs` | 标签页 | `:::tab 名称` | 子标记写 `:::col` 也可 |
| `:::duo` / `:::cols` | 双栏 | `:::col 标题` | ≤820px 自动变单列 |
| `:::detail` / `:::details` | 折叠块 | — | 标题写在标记同行，省略显示「展开查看」 |
| `:::info` 等 | Callout 的另一种写法 | — | 与 `> [!INFO]` 等价 |

结束标记统一为**单独一行的 `:::`**；容器可嵌套，按 `:::` 配对。

> [!WARNING] **容器内部不再额外缩进**
> 缩进只用来表达「标题层级」（见第六章）。标签页 / 折叠块 / 双栏**内部**的内容缩进为 0，避免面板里再叠一层无意义缩进。

## 六、正文缩进模型

渲染时，每个标题之后的内容会被包进一个 `.h-body`：

```text
<h3 id="使用步骤">使用步骤</h3>
<div class="h-body">              ← 章节内容，缩进 1 级
  <dl class="meta-card">…</dl>
  <h4 id="环境要求">环境要求</h4>
  <div class="h-body">            ← H4 的内容，缩进 2 级
    <h5 id="node-版本">Node 版本</h5>
    <div class="h-body">          ← H5 的内容，缩进 3 级
      <table>…</table>
    </div>
  </div>
</div>
```

| 规则 | 说明 |
|---|---|
| 缩进量 | 每层 1rem（窄屏 0.65rem），嵌套自动累加 |
| 标题 | 不缩进（标题属于上一层的 `.h-body`） |
| 同级闭合 | 遇到下一个同级或更高级标题时，当前 `.h-body` 自动闭合 |
| hover | 悬停任意内容时，其**祖先链**上的 `.h-body` 显示 2px 引导线 |
| 级别标记 | 标题前由 CSS 伪元素渲染 `H1`…`H6` 小字，**不进入文本**（右栏目录取标题文本时不会带上） |

## 七、代码块与高亮

1. 围栏信息串写法：三个反引号 + 语言 + `title="路径"` → 渲染为语言标签 + 标题栏 + 「复制」按钮
2. 代码先转义再交给 highlight.js；**CDN 不可用时不报错**，只是不着色
3. 高亮主题为 GitHub 明暗两套，随应用主题切换（`link[media]` 由 JS 改写）
4. 围栏**不能嵌套**：外层代码块内不能再出现单独的三个反引号行

## 八、支持与不支持

| 语法 | 状态 | 降级表现 |
|---|---|---|
| 标题 / 段落 / 引用 / 有序无序列表 / 嵌套列表 / 任务列表 | ✓ 支持 | — |
| 表格 / 分隔线 / 代码块 / 行内代码 / 粗体 / 斜体 / 链接 / 图片 | ✓ 支持 | — |
| `<br>` | ✓ 支持 | 仅放行这一个 HTML 标签，建议只用于表格单元格 |
| 表格单元格内的 `\|` | ✓ 支持 | 渲染为字面竖线 |
| `[[必填]]` / `[[选填]]` | ✓ 支持 | 其他 `[[文本]]` 渲染成默认色徽章（属误用） |
| `~~删除线~~` | ✗ 不支持 | 按字面显示两个波浪号 |
| 双反引号行内代码（用来包住反引号本身） | ✗ 不支持 | 会被拆成多个代码片段，请改用单反引号或直接用文字描述 |
| 其他 HTML 标签 | ✗ 不支持 | 被转义成文字显示 |
| 脚注 / 定义列表 / 行内 HTML | ✗ 不支持 | 按普通文本处理 |
| 元数据的数组、对象、多行值 | ✗ 不支持 | 该段不再是元数据，按普通内容渲染 |

## 九、渲染管线

```text
读取文本
  ↓ splitSections   按 H1/H2/H3 切块（跳过代码围栏内的 #），首个标题前内容并入第一章
  ↓ takeMeta        摘出标题后的 --- 元数据块（四条识别条件）
  ↓ 生成章节 id      slug(title) + 同名去重
  ↓ renderSection   标题 + 徽章 + 弃用横幅 + 元数据卡 + 正文
  ↓ parseBlocks     块级解析（nest=true 时按标题层级包 .h-body）
  ↓ renderTable     └ 折叠树 + 列语义 + 单元格样式
  ↓ renderContainer tabs / duo / details / callout
  ↓ 挂载后          bindTabs / bindTree / highlightCode / 右栏目录 / 滚动高亮
```
