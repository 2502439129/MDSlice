# API 文档规范

---
title: API 文档规范
version: V1.0.0
updated: 2026-09-16
renderer: MDSlice
scope: API接口文档.md
---

> [!INFO] **本文件自身即示例**
> 本规范按自身定义的语法书写，可直接在本应用的「打开 MD…」里打开查看渲染效果。
> 第六章给出端到端的完整示例；第五章列出所有禁止写法。

## 〇、总则

### 0.1 适用范围

本规范约束 `API接口文档.md` 的**结构**与**写法**，使其在本应用（`MDSlice.html` 入口 + `css/`、`js/` 模块组成的 MDSlice）中正确渲染。

✓ 本规范约定**内容怎么写**
✗ 不约定业务字段本身的设计（那是各集合自己的事）

### 0.2 术语

| 术语 | 含义 |
|---|---|
| 字段 | 接口的入参或返回项，即表格首列（**统一称「字段」，不再使用「参数」**） |
| 对象类型 | 需要展开成独立表格的复合类型（如 `metadata`、`FileInfo`） |
| 专用字段 | 只在某个 `type` / 分支下才出现的字段 |
| 页内小节 | H4 及以下标题，不切页，留在所属接口页面内 |

### 0.3 渲染器能力边界

本应用是一个**单页面 Markdown 阅读器**，与通用 Markdown 渲染器相比：

| 能力 | 说明 |
|---|---|
| ✓ 支持 | 标准 GFM 段落 / 列表（含嵌套与任务列表）/ 表格 / 引用 / 代码块 / 分隔线 |
| ✓ 支持 | 标题、代码块 `title`、`[[必填]]` 类徽章、Callout、`:::` 容器、`└` 折叠表格、标题级元数据 |
| ✗ 不支持 | 其他 HTML 标签（**只放行 `<br>`**）、脚注、行内 HTML、语法高亮以外的富文本 |
| ✗ 不支持 | 代码围栏嵌套（外层围栏内不能再出现单独的三个反引号行） |
| ⚠ 注意 | 一页只显示一个章节，页面由 H1/H2/H3 切分（见 §1.1） |

## 一、文档结构

### 1.1 层级与切页规则

标题层级直接决定页面切分与目录，**不能随意使用**：

| 层级 | 语义 | 渲染行为 |
|---|---|
| `#` | 文档标题，全文**有且只有一个** | 独立页面，展示**整篇内容**；元数据 `title` 作为顶栏品牌名与浏览器标题 |
| `##` | 章节：公共约定 / 各集合 / 错误码 / 附录 / 变更历史 | 独立页面，进左侧目录；页面内展示**本章及其所有子章节**的完整内容 |
| `###` | 接口：一个云函数一节 | 独立页面，进左侧目录；**仅此层级会生成右侧页内目录** |
| `####`～`######` | 页内小节：请求参数 / 调用示例 / 返回结构 … | 不切页，留在所属接口页面内；出现在右侧页内目录 |

> [!INFO] **点 `##` 进的是"一整章"**
> H1 / H2 页面会把该章下所有接口依次展开（含各自的元数据卡与弃用提示）；只想看单个接口时，点目录里的 `###`。

> [!WARNING] **接口内部禁止再用 H3**
> `###` 一律表示云函数。接口内部若写了 `###`，该部分会被切成**独立页面**，接口被拆散。

### 1.2 命名约定

1. `#` 文档标题：`API 文档规范` 这类纯名称，不带版本号
2. `##` 章节：`〇、公共约定`、`一、users 集合（用户）`、`七、统一错误码与异常处理`、`附录 A：…`、`变更历史`
3. `###` 接口：**只写函数名**（如 `addEvent`），不带任何描述；中文名放元数据 `title`
4. `####` 接口内部固定使用这三个名称：`请求参数`、`调用示例`、`返回结构`（可按需增减，如 `错误码`、`注意事项`）
5. 标题内不使用 `<br>` 与换行

### 1.3 章节骨架

```markdown
# API 文档
## 〇、公共约定
### 0.1 集合与公共字段
### 0.2 统一响应格式
## 一、users 集合（用户）
### login
### getUserProfile
## 二、events 集合（事件记录）
### addEvent
### getEvents
## 七、统一错误码与异常处理
## 附录 A：events.metadata 字段表
## 变更历史
```

> [!INFO] **不要手写目录**
> 左侧目录与右侧页内目录由应用自动生成；手写目录会变成正文内容，且与自动目录重复。

## 二、云函数标准结构

### 2.1 结构总览

一个接口（`###` 一节）由以下部分组成，顺序固定：

1. 元数据（`---` 包裹，紧随标题）
2. 描述（1～3 段正文，不使用 `####`）
3. `#### 请求参数`
   1. `##### 通用字段`
   2. `##### 专用字段`
   3. `##### 对象类型`
4. `#### 调用示例`
5. `#### 返回结构`
   1. `##### 返回字段`
   2. `##### 示例`（成功 / 失败，**推荐双栏**）

### 2.2 元数据

**语法**：紧随 `###` 标题之后（中间可空一行）的一个 `---` 包裹块，块内每行 `key: value`，**半角冒号**，值为**单行标量**，且必须有闭合 `---`。

```markdown
### addEvent

---
name: addEvent
title: 新增事件
collection: events
version: V1.0.0
operationType: [add]
deprecated: false
---

描述正文从这里开始。
```

**字段定义**：

| 字段 | 必填 | 取值 | 说明 |
|---|---|---|---|
| name | [[必填]] | 函数名 | 与 `###` 标题一致 |
| title | [[必填]] | 中文名 | 接口页显示在元数据卡片；文档首章的 `title` 额外作为顶栏品牌名 |
| collection | [[必填]] | 集合名 | 如 `events`、`users` |
| version | [[必填]] | `V MAJOR.MINOR.PATCH` | 如 `V1.2.0` |
| operationType | [[必填]] | `[add]` `[delete]` `[get]` `[update]` `[auth]` `[other]` | 写方括号会自动渲染成**彩色徽章** |
| deprecated | [[选填]] | `true` / `false` | 为 `true` 时：章节标题与左侧目录出现**弃用徽章**，页面顶部**自动插入**弃用提示（无需手写 Callout） |
| replacement | [[选填]] | 函数名 | 仅弃用接口需要；会写进自动生成的弃用提示里 |

**operationType 颜色映射**（写 `[值]` 即自动着色，无需手写样式）：

| 值 | 颜色 |
|---|---|
| `[add]` | 绿色（新增） |
| `[update]` | 黄色（变更） |
| `[get]` | 蓝色（查询） |
| `[delete]` | 红色（删除） |
| `[auth]` | 灰色（鉴权） |
| `[other]` | 灰色（其他） |

**通用徽章语法**：任意元数据的值写成 `[文本]` 形式，即渲染为徽章（默认蓝色）。

> [!WARNING] **元数据只写在标题下**
> 同一接口的元数据不要在正文里重复一遍；也不要把元数据块写在标题之前（那样只会被当作一条分隔线）。

### 2.3 描述

元数据块之后、`#### 请求参数` 之前，用 1～3 段正文说明**这个接口做什么**：

1. 不写「描述」小标题（避免与固定结构混淆）
2. 关键约束用 `[[必填]]` 之外的 Callout 强调，不要用 `**加粗**` 堆叠
3. 版本变化、设计决策等**不属于描述**，移到 `:::details` 折叠块或章节末尾

### 2.4 请求参数

```markdown
#### 请求参数

##### 通用字段

适用于所有 `type`。

| 字段 | 类型 | 必填 | 默认值 | 示例值 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `eventData` | `object` | [[必填]] | — | `{ ... }` | 嵌套对象 | 事件主体对象 |
| └ `title` | `string` | [[必填]] | — | `"晨间服药"` | 1–50 字，去空白后非空 | 事件标题 |
| └ `type` | `enum` | [[必填]] | — | `"medication"` | 见 §0.4 枚举 | 事件类型 |

##### 专用字段

按 `type` 分支分别列表（`medication` / `revisit` / `exam` …）。

##### 对象类型

每个对象类型一个 `#####` 小节 + 一个表格。
```

### 2.5 调用示例

**按调用端分标签页**，每个代码块必须标语言；文件类示例加 `title="路径"`。

```markdown
#### 调用示例

:::tabs
:::tab 小程序端
〔js 代码块，标题栏写 title="pages/event/edit.js"〕
:::
:::tab 云函数端
〔js 代码块〕
:::
:::tab HTTP / curl
〔bash 代码块〕
:::
:::
```

### 2.6 返回结构

```markdown
#### 返回结构

##### 返回字段

| 字段 | 类型 | 示例值 | 说明 |
| --- | --- | --- | --- |
| `_id` | `string` | `"evt_8f3a2c91"` | 事件唯一 ID |
| `createdAt` | `number` | `1757865600000` | 毫秒级时间戳 |

##### 示例

:::cols
:::col 成功响应
〔json 代码块〕
:::
:::col 失败响应
〔json 代码块〕
:::
:::
```

## 三、表格规范

### 3.1 入参表（7 列）

| # | 列名 | 要求 |
|---|---|---|
| 1 | 字段 | 行内代码；子字段用 `└` 前缀（见 §3.2） |
| 2 | 类型 | `string` / `number` / `boolean` / `object` / `enum` / `类型[]` / `Date` |
| 3 | 必填 | **只能是 `[[必填]]` 或 `[[选填]]`**，禁止 `✅` `❌` `是` `否` `⚠️` |
| 4 | 默认值 | 无默认值写 `—` |
| 5 | 示例值 | **可选**；可以不写，统一填 `—` |
| 6 | 校验规则 | 正则可选；简单约束写「1–50 字」「必填非空」；无则写 `—` |
| 7 | 说明 | 一句话说明，**保持简短** |

> [!WARNING] **说明与校验规则要短**
> 表格共 7 列，单元格文字过长会挤成"面条"式换行。长内容请移到 `:::details` 折叠块或正文段落。

**渲染器自动处理的单元格样式**（作者不要手写）：

| 列 | 渲染 |
|---|---|
| 字段 | 加粗等宽 |
| 类型 | 灰色等宽 |
| 示例值 / 默认值 | 形如 `"字符串"`、`[数组]`、`true`、`123` 的值显示为橙色等宽 |
| 必填 | `[[必填]]` / `[[选填]]` 渲染为徽章 |

### 3.2 折叠表格（`└`）

**语法**：在首列内容**之前**加树形字符，把该行折叠到上方的父行。

```markdown
| 字段 | 类型 | 必填 | 默认值 | 示例值 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `eventData` | `object` | [[必填]] | — | `{ ... }` | — | 事件主体 |
| └ `title` | `string` | [[必填]] | — | `"晨间服药"` | 1–50 字 | 事件标题 |
| └ `images` | `string[]` | [[选填]] | — | `["cloud://a.png"]` | ≤3 张 | 图片数组 |
| └└ `[0]` | `string` | [[必填]] | — | `"cloud://a.png"` | 合法 fileID | 数组元素 |
```

**规则**：

1. **每个树形字符计一层**：`└` 一层、`└└` 两层；`└ ├ │ ┌ ┬ ─` 都算树形字符
2. **父行 = 上方第一个层级更小的行**（通常就是不带树形字符的那行），不是"最近的不含标记的行"
3. 有子行的行会显示折叠按钮与子项数量；表格顶部会出现「共 N 个嵌套字段」与「全部折叠 / 全部展开」
4. **建议最深 3 层**；层级更深请拆表
5. 使用边界：**紧邻的嵌套**（如 `images[0]`）用 `└`；**多字段对象优先单独成表**（§3.4）

### 3.3 返回字段表

1. **至少包含三列**：`字段` `类型` `说明`
2. 有示例值时建议加 `示例值` 列
3. 返回结构中的对象嵌套同样可以用 `└`

### 3.4 对象类型表与专用字段表

1. 表头与列要求同 §3.1
2. **每个对象类型/专用字段组单独一节**（`#####`）+ 单独表格，不要挤进通用字段表
3. 节标题里写明对象名，如 `##### 对象类型 FileInfo`

### 3.5 其他表格

以下表格不做强制要求，保持简单即可：

| 用途 | 建议列 |
|---|---|
| 错误码 | `code` / `含义` / `处理建议` |
| 枚举定义 | `枚举名` / `可选值`（值之间用反斜杠转义的竖线或顿号分隔） |
| 附录字段表 | 参照 §3.1，可省略 `校验规则` |
| 变更历史 | `日期` / `变更内容` / `影响范围` / `状态` |

> [!TIP] **变更历史建议折叠**
> 变更历史的单元格通常很长，建议整表放进 `:::details` 折叠块，或改成列表形式，避免超长表格。

## 四、特殊语法

### 4.1 徽章

```markdown
| 必填 |
| --- |
| [[必填]] |
| [[选填]] |
```

1. **只允许 `[[必填]]` 和 `[[选填]]` 两个值**
2. 其他值（如 `[[重要]]`）会被渲染成蓝色默认徽章，属于误用，禁止

### 4.2 Callout 提示框

```markdown
> [!INFO] **信息标题**
> 信息正文。

> [!WARNING] **警告标题**
> 警告正文。

> [!DANGER] **危险标题**
> 危险正文。

> [!TIP] **建议标题**
> 建议正文。
```

**规则**：

1. **只允许四种类型**：`INFO`（蓝）、`WARNING`（黄）、`DANGER`（红）、`TIP`（绿）——只有这四种在应用中具备完整的图标与主题色
2. **标题必须整行加粗**（`**标题**`），否则会被当作正文；标题可与标记同行，也可以另起一行
3. 正文另起 `>` 行；与标记之间空行会**中断** Callout，不要在块内留空行
4. 普通引用（不带 `[!XXX]`）会渲染成无图标的中性浅底卡片，可用于一般引用；需要强调时一律升级为上面四种

### 4.3 折叠引用

```markdown
:::details 完整校验规则（展开查看）
- `title`：trim 后 1–50 字
- `date`：`/^\d{4}-\d{2}-\d{2}$/`
:::
```

1. `details` 的等价写法是 `detail`
2. 标题写在标记同行；省略则显示「展开查看」
3. 适合放长清单、正则、边界条件，避免撑长正文

### 4.4 标签页

```markdown
:::tabs
:::tab 小程序端
内容
:::
:::tab 云函数端
内容
:::
:::
```

1. 每个标签以 `:::` 结束，整体再以 `:::` 结束
2. 标签名写在 `:::tab` 同行，建议不超过 6 个字
3. 「调用示例」按调用端分标签；同一类示例（仅代码不同）才用标签，语义不同的内容请拆节

### 4.5 双栏

```markdown
:::cols
:::col 成功响应
内容
:::
:::col 失败响应
内容
:::
:::
```

1. `cols` 的等价写法是 `duo`
2. 典型用途：成功 / 失败响应对比
3. 窄屏（≤820px）会自动变成单列

### 4.6 代码块

```markdown
```js title="pages/event/edit.js"        ← 起始：三个反引号 + 语言 + title
const res = await wx.cloud.callFunction({ name: 'addEvent' });
```                                       ← 结束：单独一行三个反引号
```

1. **必须标语言**（`js` / `ts` / `json` / `bash` …），否则不会高亮
2. 文件、路径类示例加 `title="..."`，会显示在代码块标题栏
3. 每个代码块自带语言标签与「复制」按钮
4. **代码围栏不能嵌套**：一个代码块内不能再出现单独的三个反引号行

## 五、约束与禁忌

| # | 禁止 | 原因 / 替代 |
|---|---|---|
| 1 | 手写目录 | 侧栏与页内目录自动生成；手写目录会成为正文内容 |
| 2 | 使用 HTML 标签（`<br>` 除外） | 其他标签会被转义成字面文本显示；换行只用 `<br>`，且仅限表格单元格内 |
| 3 | 表格单元格里写裸竖线 | 会被当作列分隔符；必须转义（反斜杠 + 竖线），渲染为字面竖线 |
| 4 | 接口内部使用 H3 | 会被切成独立页面，拆散接口 |
| 5 | `[[xx]]` 写非预设值 | 只有 `必填` / `选填` 两个预设 |
| 6 | Callout 用 INFO / WARNING / DANGER / TIP 之外的类型 | 缺少图标与配色预设 |
| 7 | `---` 紧贴标题且下方是 `key: value` 形式的内容 | 会被识别成元数据块；正文分隔线请与标题隔开 |
| 8 | 元数据值跨行、写数组、写对象 | 元数据只支持单行标量 |
| 9 | `└└└└` 四级以上嵌套 | 表格过窄不可读，请拆表 |
| 10 | 表格单元格写长段落 | 7 列表格会被挤成"面条"，请移到 `:::details` |
| 11 | 手写弃用提示 | `deprecated: true` 时应用会自动生成页顶提示，重复写会得到两条 |
| 12 | 用双反引号包裹含反引号的代码 | 渲染器不支持双反引号，会被拆成多个片段；请改用单反引号，或用文字描述（如「三个反引号」） |

> [!DANGER] **元数据块的识别是"标题 + `---` + 全 `key: value` + `---`"**
> 四个条件同时满足才会被当作元数据；缺任意一条都只是普通内容（`---` 渲染为分隔线）。因此**不要**在标题下紧挨着写形如 `说明: xxx` 的段落，以免被误认为元数据。

## 六、完整示例

以一个「新增事件」接口为例，以下片段拼接即为一节的完整写法。

### 6.1 骨架

```markdown
### addEvent

---
name: addEvent
title: 新增事件
collection: events
version: V1.0.0
operationType: [add]
deprecated: false
---

为当前登录用户在指定日期下创建一条健康事件记录。

#### 请求参数
#### 调用示例
#### 返回结构
```

### 6.2 请求参数

```markdown
#### 请求参数

##### 通用字段

| 字段 | 类型 | 必填 | 默认值 | 示例值 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `eventData` | `object` | [[必填]] | — | `{ ... }` | 嵌套对象，见下 | 事件主体对象 |
| └ `title` | `string` | [[必填]] | — | `"晨间服药"` | trim 后 1–50 字 | 事件标题 |
| └ `type` | `enum` | [[必填]] | — | `"medication"` | 见枚举表 | 事件类型 |
| └ `date` | `string` | [[必填]] | — | `"2026-09-15"` | 不得晚于今天 | 发生日期 |
| └ `images` | `string[]` | [[选填]] | — | `["cloud://a.png"]` | 最多 3 张 | 图片数组 |
| └└ `[0]` | `string` | [[必填]] | — | `"cloud://a.png"` | 合法 fileID | 数组元素 |

##### 专用字段

`type = "medication"` 时以下字段位于 `eventData.metadata` 内。

| 字段 | 类型 | 必填 | 默认值 | 示例值 | 校验规则 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `drugName` | `string` | [[必填]] | — | `"二甲双胍"` | 1–50 字 | 药品名称 |
| `dose` | `string` | [[必填]] | — | `"0.5g"` | 需带单位 | 单次剂量 |
| `startAt` | `string` | [[选填]] | `date` | `"2026-09-01"` | `YYYY-MM-DD` | 开始日期 |

:::details 完整校验规则（展开查看）
- `title`：trim 后长度 1–50，禁止 `<` `>` `&`
- `date`：匹配 `/^\d{4}-\d{2}-\d{2}$/`，且不得晚于服务器当前日期
- `images`：长度 0–3，元素需为当前小程序合法的 cloud fileID
:::
```

### 6.3 调用示例

```markdown
#### 调用示例

:::tabs
:::tab 小程序端
〔js 代码块，title="pages/event/edit.js"〕
:::
:::tab 云函数端
〔js 代码块〕
:::
:::tab HTTP / curl
〔bash 代码块〕
:::
:::
```

### 6.4 返回结构

```markdown
#### 返回结构

##### 返回字段

| 字段 | 类型 | 示例值 | 说明 |
| --- | --- | --- | --- |
| `_id` | `string` | `"evt_8f3a2c91"` | 事件唯一 ID |
| `createdAt` | `number` | `1757865600000` | 毫秒级时间戳 |

##### 示例

:::cols
:::col 成功响应
〔json 代码块〕
:::
:::col 失败响应
〔json 代码块〕
:::
:::
```

### 6.5 弃用接口

```markdown
### deleteEvent

---
name: deleteEvent
title: 删除事件
collection: events
version: V1.0.0
operationType: [delete]
deprecated: true
replacement: batchDeleteEvent
---

弃用原因等补充说明写在这里；弃用提示由应用自动生成，不要手写 Callout。
```

## 七、旧文档迁移对照

把现有 `API接口文档.md` 迁到本规范时，按下表逐项替换：

| 现有写法 | 目标写法 | 数量级 |
|---|---|---|
| 手写目录（文首那份两级清单） | **删除**，目录由应用生成 | 1 处 |
| 表头「参数」 | 「字段」 | 24 个表 |
| 必填列的 `✅` / `❌` | `[[必填]]` / `[[选填]]` | ✅ 54 / ❌ 90 处 |
| 普通 `>` 引用 | 按轻重升级为 `[!INFO]` / `[!WARNING]` / `[!DANGER]` / `[!TIP]` | 77 行 |
| `⚠️` / `FIXME` / `NOTE` 提示 | `[!WARNING]` / `[!DANGER]`；长清单用 `:::details` | 32 处 + |
| 正文 `---` 分隔线 | 清理，仅保留元数据块与必要分隔 | 37 处 |
| 每个接口手写的「请求参数 / 调用示例 / 返回结构」加粗行 | 升级为 `####` / `#####` 标题 | 约 30 个接口 |
| 成功 / 失败响应两个代码块并排 | `:::cols` 双栏 | 若干 |
| 多端调用示例堆叠 | `:::tabs` 分标签 | 若干 |
| 变更历史长表格 | `:::details` 折叠 + 列表 | 1 处 |
| 缺元数据 | 每个接口补 6 字段元数据块 | 约 30 处 |
| 缺「示例值」列 | 可选补齐，缺省填 `—` | 约 28 个表 |

**迁移顺序**：

1. 应用侧按本规范完成改造（右栏页内目录、`title` 落地、`deprecated` 徽章、`ex` 列限定等）
2. 先迁「〇、公共约定」一章并在应用中验收（表格 / 徽章 / Callout / `:::` 容器全都能覆盖）
3. 再按**一个集合一批**迁移，每批迁完在应用中逐页核对目录、折叠表、Callout
4. 最后处理附录、错误码、变更历史等非接口章节
