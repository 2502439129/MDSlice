# 数学公式示例

---
title: 数学公式示例
version: V1.1.0
updated: 2026-09-17
scope: 块级与行内公式的写法、效果与边界
---

> [!INFO] **两种写法**
> **块级公式**：用一对 `$$` 包住 TeX，可以**单行**（`$$E=mc^2$$`），也可以**`$$` 单独占一行、内容跨多行**。
> **行内公式**：用一对 `$` 包住 TeX，且 **`$` 前后必须是空格、行首行尾，或强调标记**（`**` `*` `__` `_` `==` `~~`）、**内容两端不能是空格**。
> 公式由 MathJax 排版；库未加载时页面显示的就是 `$$…$$` / `$…$` 原文，不影响其余内容阅读。

## 一、块级：基本用法

### 单行

$$E = mc^2$$

### 跨多行

$$
\frac{-b \pm \sqrt{b^{2} - 4ac}}{2a}
$$

多行写法里的空行会被忽略（数学模式下空行是语法错误），其余换行原样保留。

## 二、块级：常见表达

### 上下标与希腊字母

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}, \qquad \alpha + \beta = \gamma
$$

### 矩阵

$$
\begin{pmatrix} a & b \\ c & d \end{pmatrix}
\begin{pmatrix} x \\ y \end{pmatrix}
=
\begin{pmatrix} ax + by \\ cx + dy \end{pmatrix}
$$

### 分段函数

$$
f(x) =
\begin{cases}
  x^{2}   & x \ge 0 \\
  -x      & x < 0
\end{cases}
$$

### 超宽公式（超出正文宽度时横向滚动）

$$
\int_{-\infty}^{+\infty} e^{-x^{2}} \, dx = \sqrt{\pi},
\qquad
\sum_{n=0}^{\infty} \frac{(-1)^{n}}{2n+1} = \frac{\pi}{4},
\qquad
\lim_{n \to \infty} \left(1 + \frac{1}{n}\right)^{n} = e
$$

## 三、行内公式

设直角三角形的两条直角边为 $a_1$ 与 $b_2$ 时，斜边满足 $c = \sqrt{a_1^2 + b_2^2}$ 的结论成立 —— 这样嵌在句子里也会排版。

$含 空格$

六种强调标记都能**直接包裹**公式：**$加粗$**、*$斜体$*、__$加粗$__、_$斜体$_、==$强调$==、~~$删除$~~。

标记**内侧带空格**时只有 `**` 与 `*` 生效：** $加粗$ **、* $斜体$ * 可以；== $强调$ ==、~~ $删除$ ~~ 里的标记会保持原文。

公式本身的粗斜体要用 TeX 命令： $\mathbf{A}$ 与 $\mathit{A}$ —— 外层的 `**`、`_` 改不动 MathJax 的字形，`==` 的底色则会画在公式上。

全角空格同样算作空格：　$x + y = y + x$ 没问题；行首的 $n!$ 也可以。

| 写法 | 结果 |
| --- | --- |
| `$a_1 + b_2$` | $a_1 + b_2$ |
| 紧邻标点（`（$x^2$）`、`$x^2$，`） | 不排版，按原文显示 |
| 内容两端有空格（`$ x^2 $`） | 不排版，按原文显示 |
| 强调标记直接包裹（`==$x^2$==`、`~~$x^2$~~`） | 排版且标记生效：==$x^2$== |
| 标记内侧带空格（`== $x^2$ ==`） | 公式照样排版，但标记保持原文 |

## 四、不会被当作公式的地方

### 代码块里保持原样

```text
$$
代码块里的 $$ 不会排版，原样显示
$$
```

### 行内代码里保持原样

`价格 $w$ 元` 与 `$z^2$` 都按原文显示（与代码块一致）。

### 金额、落单符号与中间夹写

金额 `$5 万和 $10 万` 不会被误判；落单的 `$` 符号、正文中间夹写的 `$$x$$` 都按原文显示。

### 转义成字面美元符

写 `\$` 得到字面的美元符：\$ 100。

### 只有落单的定界符

单独的 `$$` 一行（没有配对）会被吸收、不渲染，也不影响后面的内容。

$$
